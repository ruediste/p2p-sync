syntax = "proto3";
package p2p_sync;

import "storage.proto";

message WantUsers {
}

message HaveUsers {
  repeated bytes user_ids = 1;
}

message WantUserDatas {
  repeated bytes user_ids = 1;
}

message HaveUserDatas {
  repeated StorageUserData user_datas = 1;
}
